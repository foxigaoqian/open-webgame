name: Super Slime Site QA

on:
  push:
    branches: [main]
    paths:
      - 'examples/super-slime-black-hole-site/**'
      - 'scripts/**'
      - 'schema/**'
      - 'tests/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/super-slime-qa.yml'
  pull_request:
    branches: [main]
    paths:
      - 'examples/super-slime-black-hole-site/**'
      - 'scripts/**'
      - 'schema/**'
      - 'tests/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/super-slime-qa.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 16
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Dependency security audit
        run: npm run check:deps

      - name: Validate project config
        run: npm run check:config -- --config examples/super-slime-black-hole-site/open-webgame.json

      - name: Validate source-backed content
        run: npm run check:content -- --config examples/super-slime-black-hole-site/open-webgame.json

      - name: Validate site architecture
        run: npm run check:site -- --config examples/super-slime-black-hole-site/open-webgame.json --site-dir examples/super-slime-black-hole-site

      - name: Validate multilingual SEO
        run: npm run check:i18n -- --config examples/super-slime-black-hole-site/open-webgame.json --site-dir examples/super-slime-black-hole-site

      - name: Validate On-Page SEO
        run: npm run check:seo -- --config examples/super-slime-black-hole-site/open-webgame.json --html examples/super-slime-black-hole-site/index.html --site-dir examples/super-slime-black-hole-site

      - name: Validate iframe permissions
        run: npm run check:security -- --config examples/super-slime-black-hole-site/open-webgame.json --html examples/super-slime-black-hole-site/index.html

      - name: Validate embed configuration
        run: npm run verify:embed:config -- --config examples/super-slime-black-hole-site/open-webgame.json

      - name: Run regression tests
        run: npm test

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run browser and accessibility QA
        run: npm run qa:browser -- --config examples/super-slime-black-hole-site/open-webgame.json --site-dir examples/super-slime-black-hole-site --artifacts qa-artifacts/super-slime-browser

      - name: Run Lighthouse QA
        run: npm run qa:lighthouse -- --config examples/super-slime-black-hole-site/open-webgame.json --site-dir examples/super-slime-black-hole-site --artifacts qa-artifacts/super-slime-lighthouse

      - name: Upload QA artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: super-slime-quality-artifacts
          path: qa-artifacts/
          if-no-files-found: warn
          retention-days: 14
